apiVersion: v1
kind: Service
metadata:
  name: backend-ventas-service
  namespace: despachos-prod
  labels:
    app: backend-ventas
spec:
  type: ClusterIP
  selector:
    app: backend-ventas
  ports:
    - name: http
      port: 8080
      targetPort: 8080
